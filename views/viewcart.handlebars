<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/zak.css">
    <link rel="stylesheet" href="./css/simple-grid.css">
    <title>viewcart</title>
</head>

<body>
    <div class="container">
        <h4>My Cart</h4>
        <div class="row">
            <div class="col-12">
                <table>
                    <tr>

                        <th> items selected</th>
                        <th> items price</th>
                        {{!-- <td><span class="itemPrice"></span> --}}
                    


                    </tr>
                    {{#each cart.items }}
                    <tr>
                        <td>{{description}}<span class="itemSelected"></span></td>
                        <td>R{{price}}<span class="itemSelected"></span></td>
                    </tr>

                    {{/each}}

                    <tr>
                        <td> total cost</td>
                        <td>R<span class="totalCost"> {{cart.total}} </span></td>

                    </tr>


                </table>



                <Button>CLEAR CART</Button>

                <Button>CHECKOUT</Button>
            </div>

        </div>
</body>

</html>